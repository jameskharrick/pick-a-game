{
  "buildCommand": "npm run build --prefix client",
  "outputDirectory": "client/dist",
  "functions": {
    "server/index.js": {
      "maxDuration": 10
    }
  },
  "rewrites": [
    { "source": "/api/:path*", "destination": "/server/index" },
    { "source": "/auth/:path*", "destination": "/server/index" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
